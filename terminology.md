# Project Terminology

- **database**: The Postgres instance we're connecting to and synchronizing.
- **server**: The `pgreflex` (C# backend).
- **client**: The Node.js application webserver (the intermediary that the frontend connects to).
- **frontend**: The application / JavaScript running in the user's browser.
